// Méthode appelée dès que l'utilisateur modifie sa saisie.
function retrieve() {
    var saisie = document.getElementById("bsearch").value;
    retrieve(saisie);
}


function retrieve(saisie) {
    
}

